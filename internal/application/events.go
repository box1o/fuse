package application

// import "fuse/pkg/log"

func (a *Application) dispatchEvents() error {

	// if err := a.eventManager.Bus().Subscribe("test.notification", a.notificationSvc.SubscribeEventTest); err != nil {
	// 	log.Error("Failed to subscribe to test.notification event: %v", err)
	// 	return err
	// }
	//
	return nil
}
